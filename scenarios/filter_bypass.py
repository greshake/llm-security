"""
This example shows how a two-stage filter can be bypassed.
The Chat Agent in this example can only see a filtered summary of
web request data. Another smaller GPT model is asked to sanitize the
incoming, untrusted data.
"""